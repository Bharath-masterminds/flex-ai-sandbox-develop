"""
Liquid Mapper Use Case - FHIR Liquid Template and Mapping Table Generator

This use case provides a Streamlit UI for converting between:
- Liquid mappings → Mapping table documentation (Step 0)
- Input JSON + references → Mapping table documentation (Step 1)
- Input JSON + mapping tables → Liquid templates (Step 2)
"""
